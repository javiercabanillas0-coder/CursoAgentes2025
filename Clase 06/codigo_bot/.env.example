# Ejemplo de archivo .env para este proyecto
# Copia este archivo a `.env` y reemplaza los valores por tus claves reales.
# Este archivo contiene valores ficticios / placeholders. NO uses estos valores en producción.

# --- API Keys / AI Providers (placeholders) ---
OPENAI_API_KEY="sk-REEMPLAZAR_OPENAI_API_KEY"

GOOGLE_API_KEY="AIzaREEMPLAZAR_GOOGLE_API_KEY"
GROQ_API_KEY="gsk-REEMPLAZAR_GROQ_API_KEY"
ANTHROPIC_API_KEY="sk-ant-REEMPLAZAR_ANTHROPIC_KEY"
DEEPSEEK_API_KEY="sk-REEMPLAZAR_DEEPSEEK_KEY"

# --- Weather / otras APIs ---
WEATHER_API_KEY="REEMPLAZAR_WEATHER_API_KEY"

# --- Pushover (placeholders) ---
PUSHOVER_USER="REEMPLAZAR_PUSHOVER_USER"
PUSHOVER_TOKEN="REEMPLAZAR_PUSHOVER_TOKEN"

# --- Hugging Face Token (placeholder) ---
HF_TOKEN=hf_xxx

# --- Notas ---
# - Copia este archivo a `.env` y sustituye los valores por tus claves reales.
# - Asegúrate de no commitear tu archivo `.env` al repositorio (añádelo a .gitignore).
# - Si quieres, puedo buscar en el repositorio otras ocurrencias de claves reales y redacterlas.